import { Text, View } from "react-native"
import { SafeAreaView } from "react-native-safe-area-context";

const Chat = () => {
    return (
        <SafeAreaView>
            <Text>Richard udeh</Text>
        </SafeAreaView>
    )
}
export default Chat;